import {PublicShell} from '@/components/public-shell';import {categories} from '@/lib/services';import {ArrowRight} from 'lucide-react';import Link from 'next/link';
export default function Services(){return <PublicShell><div className="pageHead"><div className="shell"><div className="eyebrow">Service catalog</div><h1>Technology services without artificial limits.</h1><p className="sub">Pick one service, several services, or let SolidITPro design a complete environment.</p></div></div><section className="section"><div className="shell grid3">{categories.map(c=><div className="card" key={c.slug}><div className="kicker">{c.services.length} capabilities</div><h3>{c.name}</h3><p>{c.tagline}</p><div style={{marginTop:16}}>{c.services.slice(0,7).map(s=><div className="check" key={s}>✓ <span>{s}</span></div>)}</div>{c.services.length>7&&<div className="muted" style={{fontSize:13}}>+ {c.services.length-7} more services</div>}<Link className="serviceLink" href="/build-your-solution">Add to solution <ArrowRight size={16}/></Link></div>)}</div></section></PublicShell>}