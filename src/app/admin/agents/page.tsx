import {AdminShell} from '@/components/admin-shell';import {agents} from '@/lib/agents';
export default function Agents(){return <AdminShell active="AI Agents"><div className="topline"><div><div className="kicker">Agent workforce</div><h1>AI Agents</h1><p className="muted">Specialized agents operate through the same auditable work queue as humans.</p></div><button className="btn">+ Configure agent</button></div><div className="grid3">{agents.map(a=><div className="card" key={a.key}><span className="badge green">Enabled</span><h3>{a.name}</h3><p>{a.role}</p><div className="serviceLink"><span>Provider: configurable</span><span>→</span></div></div>)}</div></AdminShell>}