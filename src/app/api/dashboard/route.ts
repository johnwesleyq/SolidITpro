import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function GET(){try{const [customers,tickets,projects,work,approvals]=await Promise.all([db.organization.count(),db.ticket.count({where:{status:{not:'CLOSED'}}}),db.project.count({where:{status:{not:'completed'}}}),db.workItem.count({where:{status:{not:'CLOSED'}}}),db.approval.count({where:{status:'pending'}})]);return NextResponse.json({customers,tickets,projects,work,approvals})}catch{return NextResponse.json({customers:42,tickets:18,projects:11,work:13,approvals:2,mode:'demo'})}}