import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{const w=await db.workItem.findUnique({where:{id},include:{events:true,approvals:true}});return w?NextResponse.json(w):NextResponse.json({error:'not found'},{status:404})}catch{return NextResponse.json({error:'database unavailable'},{status:503})}}