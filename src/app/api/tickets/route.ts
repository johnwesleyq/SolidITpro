import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function GET(){try{return NextResponse.json(await db.ticket.findMany({orderBy:{createdAt:'desc'},take:100}))}catch{return NextResponse.json([])}}export async function POST(req:Request){const b=await req.json();if(!b.organizationId||!b.subject)return NextResponse.json({error:'organizationId and subject required'},{status:400});return NextResponse.json(await db.ticket.create({data:{organizationId:b.organizationId,subject:b.subject,description:b.description||'',priority:b.priority||'normal'}}),{status:201})}