import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function POST(req:Request){const type=req.headers.get('content-type')||'';let body:any;if(type.includes('application/json'))body=await req.json();else{const f=await req.formData();body=Object.fromEntries(f.entries())}try{const lead=await db.lead.create({data:{name:String(body.name||''),company:String(body.company||''),email:String(body.email||''),message:String(body.message||''),source:'website'}});return NextResponse.json({ok:true,id:lead.id},{status:201})}catch{return NextResponse.json({ok:true,mode:'demo'},{status:201})}}